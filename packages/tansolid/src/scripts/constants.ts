export const CLI_NAME = 'tansolid';
export const DEFAULT_ROOT = `.${CLI_NAME}`;
export const JSON_PATH = `.${CLI_NAME}.bemedev.json`;

export const config = {
  json: JSON_PATH,
  root: DEFAULT_ROOT,
};
